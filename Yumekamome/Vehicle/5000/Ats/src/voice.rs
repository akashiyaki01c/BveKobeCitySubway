use bveats_rs::AtsVehicleState;

#[derive(Default)]
pub struct VoiceManager {

}

impl VoiceManager {
	/// 音声関連のループ関数
	pub fn elapse(&mut self, state: AtsVehicleState, panel: &mut [i32], sound: &mut [i32]) {

	}
}